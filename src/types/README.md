# src/types/

Ambient type declarations for packages that ship no types.

## Contents

- `pagedjs.d.ts` — minimal typings for `pagedjs` (`Previewer.preview`, `registerHandlers`), covering only the surface used by `src/print/`.
